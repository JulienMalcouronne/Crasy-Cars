<h1>All our cars</h1>
<h4><%= "all #{@cars.length} cars"%></h4>
<ul>
    <% @cars.each do |car|%>
    <li class="">
      <%# <img src="<%= car.photo %>
      <%= car.name %>
      <br>
      <%= car.address %>
      <br>
      <%= car.summary %>
      <br>
      <%= "€ #{car.price}" %>
      <br>
      <%= "#{car.capacity} guests maximum" %>
      <br>
      <%= link_to "All cars", cars_path %>
    </li>
  <% end %>

</ul>
